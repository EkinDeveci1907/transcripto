# OpenAI API key for Whisper and GPT (leave empty when using USE_MOCK=true)
OPENAI_API_KEY=

# Optional: override models
OPENAI_SUMMARY_MODEL=gpt-4o-mini
OPENAI_WHISPER_MODEL=whisper-1

# Enable to run without external APIs (returns placeholder transcript/summary)
USE_MOCK=true